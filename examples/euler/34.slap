-- Project Euler #34: Digit Factorials
-- Sum of numbers equal to the sum of factorial of their digits
-- Answer: 40730

(1 1 2 6 24 120 720 5040 40320 362880) 'facts let

-- stack: total n
0 3
(dup 2540161 lt)
(
  -- stack: total n
  dup 0 swap
  -- stack: total n acc dn
  (dup 0 gt)
  (
    -- stack: total n acc dn
    dup 10 mod 'facts swap nth must swap (plus) dip
    -- stack: total n acc' dn
    10 div
    -- stack: total n acc' dn'
  ) while
  drop
  -- stack: total n dfs
  over eq (dup (plus) dip) () if
  1 plus
) while
drop print
