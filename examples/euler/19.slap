-- Project Euler #19: Counting Sundays
-- How many Sundays fell on the first of the month from 1 Jan 1901 to 31 Dec 2000?
-- Answer: 171

'is-leap (
  'y let
  y 400 mod 0 eq (1) (
    y 100 mod 0 eq (0) (
      y 4 mod 0 eq
    ) if
  ) if
) def

[31 28 31 30 31 30 31 31 30 31 30 31] 'mdays let

'days-in-month (
  'mo let 'yr let
  mdays mo get
  mo 1 eq yr is-leap and (1 plus) () if
) def

'step-month (
  dup 'y at 1901 ge over 'dow at 0 eq and ('cnt (inc) edit) () if
  dup 'y at over 'm at days-in-month over 'dow at plus 7 mod 'dow into
  dup 'm at 11 eq
    ('y (inc) edit 'm (drop 0) edit)
    ('m (inc) edit)
  if
) def

rec 0 'cnt into 1 'dow into 1900 'y into 0 'm into
1212 (step-month) repeat
'cnt at print
