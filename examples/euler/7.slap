-- Project Euler #7: 10001st Prime
-- What is the 10001st prime number?
-- Answer: 104743

'is-prime (
  'n let
  n 2 lt (0) (
    2 'i let
    1 'result let
    (i i mul n le result and)
    (
      n i mod 0 eq (0 'result let) () if
      i 1 plus 'i let
    )
    while
    result
  ) if
) def

0 'cnt let
1 'candidate let

(cnt 10001 lt)
(
  candidate 1 plus 'candidate let
  candidate is-prime (cnt 1 plus 'cnt let) () if
)
while

candidate print
